#define _CRT_SECURE_NO_WARNINGS 1

# include<stdio.h>

double average(double i, double j)
{
	return 1 / ((1 / i + 1 / j) / 2);
}


int main()
{
	double m, n;
	printf("输入两个数：");
	scanf("%lf%lf", &m, &n);
	printf("调和平均数为：%lf", average(m, n));
	return 0;
}
