#define _CRT_SECURE_NO_WARNINGS 1

# include<stdio.h>
int main()
{
	int i = 0;
	int s = 0;
	int m = 0;
	int n = 0;
	int arr[10] = { 0 };
	while (i < 10)
	{
		scanf("%d", &arr[i]);
		
		i++;
	}
		
				
		while (s<=9)
	{
			while(m <= 9)

			{
				if (arr[s] != arr[m])
					m++, n++;
				   
				else m++;
			}

			if (n == 9)
				printf("%d ", arr[s]), s++;

			else
				s++;
			   m = 0,n=0;
	}
	return 0;
}
