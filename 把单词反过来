#define _CRT_SECURE_NO_WARNINGS 1

# include<stdio.h>
#include<string.h>

int main()
{
	int l,i;
	char arr[20] = { 0 };
	printf("输入一个单词：");
	scanf("%s", arr);
	l = strlen(arr);
	for (i = l; i >= 0; i--)
	{
		printf("%c", arr[i]);
	}
	return 0;
}
